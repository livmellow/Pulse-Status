"use client";
import { useState } from "react";
export default function Home(){const[view,setView]=useState<"home"|"setup">("home");const[step,setStep]=useState(1);const[reason,setReason]=useState("No incident reported");return <main>{view==="home"?<><nav><div className="brand"><img src="/pulse-status-logo.png" alt="Pulse Status"/>PULSE<span>STATUS</span></div><div><button className="plain">Log in</button><button className="primary" onClick={()=>setView("setup")}>Start free</button></div></nav><section className="hero"><p className="eyebrow">HOSTED STATUS PAGES THAT LOOK LIKE YOUR BRAND</p><h1>Know what is down.<br/><em>Tell people why.</em></h1><p>Pulse Status monitors your websites, APIs, servers, and Discord bots. Build a beautiful status page your customers can trust.</p><div className="hero-actions"><button className="primary" onClick={()=>setView("setup")}>Create your status page</button><button className="plain">See how it works</button></div></section><section className="preview"><div className="preview-top"><div><b>northstar</b><span>status</span></div><span className="ok">All systems operational</span></div><h2>Northstar Studio</h2><p className="muted">Live service status</p><article><div className="row"><b>Website</b><span className="ok">Operational</span></div><div className="bars">{Array.from({length:28},(_,i)=><i key={i}/>)}</div><small>99.99% uptime · checked every 60 seconds</small></article><article><div className="row"><b>Discord bot</b><span className="warn">Maintenance</span></div><div className="incident"><b>Why is it down?</b><p>Scheduled database maintenance. Bot commands will return shortly.</p></div><small>Incident created 12 minutes ago</small></article></section><section className="features"><div><span>01</span><h3>Your own brand</h3><p>Use your name, logo, domain, colors, and official website link.</p></div><div><span>02</span><h3>Real health checks</h3><p>Monitor an IP address, domain, URL, port, or bot health endpoint.</p></div><div><span>03</span><h3>Clear incidents</h3><p>Automatically detect downtime, then show customers exactly what happened.</p></div></section><footer>Pulse Status · Status pages made simple</footer></>:<section className="setup"><button className="back" onClick={()=>setView("home")}>Back to home</button><div className="setup-head"><img src="/pulse-status-logo.png" alt=""/><div><p className="eyebrow">CREATE YOUR STATUS PAGE</p><h2>Let's get you set up.</h2></div></div><div className="steps"><span className={step>=1?"on":""}>1 Brand</span><span className={step>=2?"on":""}>2 Add monitor</span><span className={step>=3?"on":""}>3 Publish</span></div>{step===1&&<div className="card"><h3>Make the page yours</h3><label>Public page name<input placeholder="Acme Status"/></label><label>Official website<input placeholder="https://acme.com"/></label><label>Custom domain<input placeholder="status.acme.com"/></label><label>Brand color<input type="color" defaultValue="#467c51"/></label><button className="primary" onClick={()=>setStep(2)}>Continue</button></div>}{step===2&&<div className="card"><h3>Add your first monitor</h3><label>Monitor type<select><option>Website / HTTP health check</option><option>Discord bot health endpoint</option><option>TCP port</option><option>Ping / IP address</option></select></label><label>Health URL, IP address, or domain<input placeholder="https://bot.acme.com/health"/></label><label>Check every<select><option>60 seconds</option><option>5 minutes</option><option>15 minutes</option></select></label><p className="tip">For a Discord bot, point this at a /health route from the bot host. It should only return OK when the bot is connected to Discord.</p><button className="primary" onClick={()=>setStep(3)}>Continue</button></div>}{step===3&&<div className="card"><h3>Incident messaging</h3><label>Current incident reason<textarea value={reason} onChange={e=>setReason(e.target.value)}/></label><p className="tip">When a check fails, Pulse Status will mark the service down automatically. You can publish this message to explain the outage.</p><button className="primary">Publish status page</button></div>}</section>}</main>}
