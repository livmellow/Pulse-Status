"use client";
import {useState} from "react";
export default function Home(){const[view,setView]=useState<"landing"|"dashboard"|"create">("landing");const[modal,setModal]=useState(false);return <main>{view==="landing"?<><nav><a className="brand"><span className="mark">P</span>pulse<span>status</span></a><div className="navlinks"><a href="#pricing">Pricing</a><button className="login" onClick={()=>setModal(true)}>Log in</button><button className="cta small" onClick={()=>setModal(true)}>Start free</button></div></nav><section className="hero"><div className="eyebrow live"><i/> Built for people who care about uptime</div><h1>Your services deserve<br/><em>a calmer status page.</em></h1><p>Monitor your websites, APIs, ports, and bots. Give customers clear answers when something changes.</p><div className="heroButtons"><button onClick={()=>setModal(true)} className="cta">Create a status page →</button><a href="#pricing" className="watch">See pricing</a></div></section><section className="product"><div className="browser"><div className="browserbar"><span/><span/><span/><b>status.yourcompany.com</b><label>● All systems operational</label></div><div className="statuscontent"><small>YOUR COMPANY</small><h2>Service status</h2><Service name="Website & API" detail="99.99% uptime · 43ms"/><Service name="Discord bot" detail="Gateway connected · 57ms"/></div></div></section><section className="proof"><div><b>5</b><span>monitors on Starter</span></div><div><b>60s</b><span>fastest check interval</span></div><div><b>90d</b><span>Starter history</span></div><div><b>24/7</b><span>automatic monitoring</span></div></section><section id="pricing" className="pricing"><div><span>ONE SIMPLE PLAN</span><h2>Good uptime should<br/>not cost a fortune.</h2><p>Start free. Upgrade when you need more visibility.</p></div><div className="pricecard"><b>Starter</b><div className="price"><strong>$5</strong><span>/ month</span></div><ul><li>5 monitors, checked every minute</li><li>1 branded public status page</li><li>Custom domain support</li><li>90 days of uptime history</li></ul><button className="cta" onClick={()=>setModal(true)}>Get started free</button><small>Free: 1 monitor and 7-day history</small></div></section></>:view==="dashboard"?<Dashboard onCreate={()=>setView("create")} onExit={()=>setView("landing")}/>:<Create onBack={()=>setView("dashboard")}/>} {modal&&<div className="modal"><div><button className="x" onClick={()=>setModal(false)}>×</button><span>WELCOME TO PULSE STATUS</span><h2>Start in test mode</h2><p>Try the workspace without setting up login yet.</p><button className="cta" onClick={()=>{setModal(false);setView("dashboard")}}>Continue in test mode</button><button className="google">Email & Google sign-in coming soon</button><small>Your test workspace is only for previewing the setup flow.</small></div></div>}</main>}
function Dashboard({onCreate,onExit}:{onCreate:()=>void;onExit:()=>void}){return <section className="dashboard"><nav><div className="brand"><span className="mark">P</span>pulse<span>status</span></div><button className="login" onClick={onExit}>Exit test mode</button></nav><div className="dashhead"><div><span>TEST WORKSPACE</span><h1>Welcome to Pulse Status</h1><p>This is your command center. Create a page when you’re ready.</p></div><button className="cta" onClick={onCreate}>+ Add status page</button></div><div className="empty"><span className="bigmark">P</span><h2>Create your first status page</h2><p>Set up a branded page, then add monitors for your services. Nothing is public until you publish it.</p><button className="cta" onClick={onCreate}>Add status page</button><small>You can skip details and edit them later.</small></div></section>}
function Create({onBack}:{onBack:()=>void}){return <section className="dashboard"><nav><div className="brand"><span className="mark">P</span>pulse<span>status</span></div><button className="login" onClick={onBack}>Back to dashboard</button></nav><div className="formcard"><span>NEW STATUS PAGE</span><h1>Make it yours.</h1><p>Start with the basics. Everything can be changed later.</p><label>Page name<input placeholder="Acme Status"/></label><label>Public address<input placeholder="status.acme.com"/></label><label>Brand color<input type="color" defaultValue="#397349"/></label><label>First monitor type<select><option>Website or health-check URL</option><option>Discord bot health endpoint</option><option>IP address or TCP port</option></select></label><button className="cta" onClick={onBack}>Save draft</button></div></section>}
function Service({name,detail}:{name:string;detail:string}){return <div className="service"><div><b><i/> {name}</b><p>{detail}</p></div><div className="blocks">{Array.from({length:26},(_,i)=><span key={i}/>)}</div><label>Operational</label></div>}
